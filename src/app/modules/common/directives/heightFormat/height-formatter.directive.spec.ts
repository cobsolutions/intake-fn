import { HeightFormatterDirective } from './height-formatter.directive';

describe('HeightFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
